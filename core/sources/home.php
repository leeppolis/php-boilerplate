<?php
	/*
	 *
	 * Homepage.
	 * This is the homepage of the Magazine.
	 * If you keep the default functionalities, you should not need to edit anything.
	 * Otherwise, remember that anything that you want to make visible in the template
	 * must be added to the $output object.
	 * $output->head, $output->magazine, and and $output->properties have some
	 * keywords reserved,have a look at them with print_r( $output ); or read
	 * the docs.
	 *
	 * This example homepage shows the current issue with its
	 * M_MAX_ARTICLES_PER_ISSUE articles and the 3 previous issues, with links.
	 *
	 */
	 
	echo('Welcome to my homepage');